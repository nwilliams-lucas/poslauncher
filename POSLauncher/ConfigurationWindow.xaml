<Window x:Class="POSLauncher.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuration" 
        Height="350" 
        Width="500"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Commerce Client">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Commerce_Client Application Path:" 
                               Margin="0,0,0,5"/>
                    <Grid Grid.Row="1" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ClientPathTextBox" 
                                 Grid.Column="0" 
                                 Height="25"/>
                        <Button x:Name="BrowseButton" 
                                Grid.Column="1" 
                                Content="Browse..." 
                                Width="80" 
                                Height="25" 
                                Margin="5,0,0,0"
                                Click="BrowseButton_Click"/>
                    </Grid>

                    <TextBlock Grid.Row="2" 
                               Text="Command Line Arguments:" 
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="ArgumentsTextBox" 
                             Grid.Row="3" 
                             Height="25" 
                             Margin="0,0,0,15"/>

                    <GroupBox Grid.Row="4" Header="Instructions">
                        <TextBlock TextWrapping="Wrap" Margin="5">
                            The application will automatically try to find the Commerce_Client shortcut on the desktop to extract the path and parameters. You can manually specify them here if needed.
                            <LineBreak/><LineBreak/>
                            Note: Different machines may require different parameters. The configuration is stored locally for each machine.
                        </TextBlock>
                    </GroupBox>
                </Grid>
            </TabItem>

            <TabItem Header="Services">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" 
                               Text="Service Configuration:" 
                               FontWeight="Bold" 
                               Margin="0,0,0,10"/>

                    <StackPanel Grid.Row="1">
                        <CheckBox x:Name="AutoStartCheckBox" 
                                  Content="Enable automatic startup on boot" 
                                  Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Service Start Timeout (seconds):" 
                                   Margin="0,0,0,5"/>
                        <TextBox x:Name="TimeoutTextBox" 
                                 Width="100" 
                                 Height="25" 
                                 HorizontalAlignment="Left" 
                                 Margin="0,0,0,15"/>

                        <GroupBox Header="Service Information">
                            <TextBlock TextWrapping="Wrap" Margin="5">
                                PostgreSQL: Automatically detects common PostgreSQL service names
                                <LineBreak/>
                                JMC FIXED: Looks for service named "fixed" with display name "JMC FIXED"
                                <LineBreak/><LineBreak/>
                                Services are started in order: PostgreSQL first, then JMC FIXED, then Commerce_Client application.
                            </TextBlock>
                        </GroupBox>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,15,0,0">
            <Button x:Name="SaveButton" 
                    Content="Save" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>